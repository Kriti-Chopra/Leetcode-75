class Solution {
    public boolean containsDuplicate(int[] nums) {
        
        HashMap<Integer,Integer> map = new HashMap<>();

        //if map has entry that means num appeared again so return true
        for(int num : nums){
            if(!map.containsKey(num)) map.put(num,0);
            else return true;
        }
        
        return false;
    }
}
